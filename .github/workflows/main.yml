name: macOS Remote VNC via Tailscale

on:
  workflow_dispatch:

jobs:
  remote-desktop:
    runs-on: macos-latest

    steps:
      - name: Récupérer le code du dépôt
        uses: actions/checkout@v4

      # 1. Connexion Tailscale
      - name: Connexion au réseau Tailscale
        uses: tailscale/github-action@v4
        with:
          authkey: ${{ secrets.TAILSCALE_AUTH_KEY }}

      # 2. Configuration complète du VNC
      - name: Activer le VNC macOS
        run: |
          echo "=== Création de l'utilisateur ==="
          sudo dscl . -create /Users/vncuser
          sudo dscl . -create /Users/vncuser UserShell /bin/bash
          sudo dscl . -create /Users/vncuser RealName "VNC User"
          sudo dscl . -create /Users/vncuser UniqueID 502
          sudo dscl . -create /Users/vncuser PrimaryGroupID 20
          sudo dscl . -create /Users/vncuser NFSHomeDirectory /Users/vncuser
          sudo dscl . -passwd /Users/vncuser "MonMotDePasseSecurise123!"
          sudo createhomedir -c -u vncuser
          sudo dscl . -append /Groups/admin GroupMembership vncuser

          echo "=== Activation du VNC ==="
          sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist

          echo "=== Préparation Auto-Login ==="
          sudo defaults write /Library/Preferences/com.apple.loginwindow AutoLoginUser "vncuser"

      # 3. Étape Python pour le mot de passe
      - name: Générer le fichier de mot de passe Auto-Login
        shell: python
        run: |
          import os
          os.system("sudo kcpassword MonMotDePasseSecurise123! || true")

      # 4. Redémarrage graphique sécurisé (Évite l'erreur 143)
      - name: Redémarrer le serveur graphique en tâche de fond
        run: |
          # Le "nohup ... &" permet de lancer la commande en tâche de fond 
          # sans que GitHub Actions ne se fasse couper la chique
          sudo nohup killall loginwindow > /dev/null 2>&1 &
          # On attend 5 secondes que le serveur graphique se relance tranquillement
          sleep 5

      # 5. Maintien de la machine active
      - name: Maintenir la session active (Timeout 1 heure)
        run: |
          echo "=========================================================="
          echo "  Machine macOS prête ! Connecte-toi via Tailscale."
          echo "  Identifiant de connexion VNC : vncuser"
          echo "  Mot de passe VNC             : MonMotDePasseSecurise123!"
          echo "=========================================================="
          sleep 3600
